class CardGame:


  def check_for_ace(self, card):
    # = should be == to check equality
    if card.value = 1:
      return True
    # need a : after else
    else
      return False
   
    # dif should be def
    # should have a comma between card1 and card2 
  dif highest_card(self, card1 card2):
  # need to indent 4 lines below this to be within highest_card method
  if card1.value > card2.value:
    # card should be card1
    return card
  else:
    return card2
  

# all lines in this function before the return should be indented, return should be in line with for
def cards_total(self, cards):
  # total needs to be assigned to 0
  total
  for card in cards:
    total += card.value
    # need to make total a string, e.g. str(total) to concatenate
    # should have a space between of and " to separate the total 
    return "You have a total of" + total
  